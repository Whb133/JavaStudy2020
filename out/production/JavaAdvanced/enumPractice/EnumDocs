枚举类：

一.枚举类的使用
    1.枚举类的理解：类的对象只有确定的有限个，我们称此类为枚举类。
    2.当需要定义一组常量时，建议使用枚举类。
    3.如果枚举类中只有一个对象，则可以使用单例模式作为其实现方式。
二.如何定义枚举类
    方式一：Before jdk1.5，自定义枚举类
            class Season
    方式二：Since jdk1.5，可以使用enum关键字定义枚举类
            class Season1
三.Enum中常用方法
    1.toString(）  返回当前枚举类对象常量的名称
    2.values()   返回枚举类中定义的对象数组，该方式可以遍历枚举类所有枚举值。
    3.valueOf(String objName)   根据提供的objName，返回枚举类中对象名为objName的对象，若未找到会抛异常。
四.使用enum关键字定义的枚举类实现接口的情况
    1.实现接口，在枚举类中重写抽象方法。
    2.让枚举类对象分别重写接口中的抽象方法。